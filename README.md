# Project_Climate_display 
    This is for indivual project for SED 1115. 
   To use another CSV file, download a file from (https://climate.weather.gc.ca/historical_data/search_historic_data_e.html). Make sure it is a CSV file and name it "climate.csv" Then, it should work
 to install: python libraies
 ```bash
python -m pip install -r requirements.txt